# dbt_diagnostics/config.yml
# Operational config only. No type opinions -- this is a debugger.

project:
  dbt_project_dir: .

connection:
  method: profiles
  profile_name: default
  target_name: dev
